import { Loading } from "@/components/shared/Loading";

const LoadingPage = () => {
  return (
    <div className="h-full flex flex-col justify-center items-center px-10 py-20">
      <Loading />
    </div>
  );
};

export default LoadingPage;
